---
import "../styles/global.css";

import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "About Me";

const identity = {
  name: "Kanishka Chowdhury",
  country: "India",
  occupation: "Software Engineer",
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Writing Docs"];
const skillColor = "navy";
const skillTransform = "uppercase";
---

<style define:vars={{ skillColor, skillTransform }}>
  h1 {
    color: purple;
    font-size: 4rem;
  }

  .skill {
    color: var(--skillColor);
    font-weight: bold;
    text-transform: var(--skillTransform);
  }
</style>
<BaseLayout pageTitle={pageTitle}>
  <h2>... and my new Astro site!</h2>

  <p>
    I am working through Astro's introductory tutorial. This is the second page
    on my website, and it's the first one I built myself!
  </p>

  <p>
    This site will update as I complete more of the tutorial, so keep checking
    back and see how my journey is going!
  </p>

  <p>Few facts about me:</p>
  <ul>
    <li>I am {identity.name} from {identity.country}</li>
    <li>I work as a {identity.occupation}</li>
  </ul>

  <p>My skills are:</p>
  <ul>
    {skills.map((skill) => <li class="skill">{skill}</li>)}
  </ul>
</BaseLayout>
